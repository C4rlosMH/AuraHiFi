import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    footerRow: {
        flexDirection: 'row',
        justifyContent: 'space-around',
        alignItems: 'center',
        paddingBottom: 10,
    },
    footerAction: {
        padding: 10,
    }
});